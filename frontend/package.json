{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.21.1", // HTTP client for making requests
    "react": "^17.0.2", // React library
    "react-dom": "^17.0.2", // React DOM library
    "react-scripts": "4.0.3" // Scripts for running React app
  },
  "scripts": {
    "start": "react-scripts start", // Start development server
    "build": "react-scripts build", // Build the project
    "test": "react-scripts test", // Run tests
    "eject": "react-scripts eject" // Eject configuration (optional)
  }
}
